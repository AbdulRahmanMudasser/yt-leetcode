class Solution {
  String toLowerCase(String s) {
    // StringBuffer sb = StringBuffer();

    // for (int i = 0; i < s.length; i++) {
    //     int charCode = s.codeUnitAt(i);

    //     if (charCode >= 65 && charCode <= 90) {
    //         sb.write(String.fromCharCode(charCode + 32));
    //     } else {
    //         sb.write(s[i]);
    //     }
    // }

    // return sb.toString();

    return s.toLowerCase();
  }
}
